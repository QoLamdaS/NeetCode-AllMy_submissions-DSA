class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        while True:
            for i in nums:
                target - i
                for j in nums:
                    target - j 
            if i + j == target:
                return False
            
